import std::algo

fn main() -> Int ::
    let top_sum := algo.top_k_sum_repeat([9, 1, 8, 2, 7, 3, 6, 4, 5], 4, 20000)
    let split_sum := algo.split_test_count_repeat(20, 0.2, 42, 1, 20000)
    if top_sum <= 0 ::
        return 1
    ::
    if split_sum != 80000 ::
        return 1
    ::
    return 0
::

main()
