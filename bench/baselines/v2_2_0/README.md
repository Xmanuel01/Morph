# v2.2.0 Frozen Baseline

This directory stores the pre-recovery benchmark baseline used by the v2.2.1-v2.3.0 recovery track.

- `pre_recovery_baseline.json`: frozen summary from the failing mixed-median gate before fairness and runtime-path fixes.

Do not rewrite these artifacts during optimization runs. Add new benchmark outputs under `bench/results/` and compare against this baseline.
