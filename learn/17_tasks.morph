// Expected: prints 42
fn work() -> Int ::
    task.sleep(10)
    return 42
::

let h := task.spawn(work)
print(task.join(h))

// Error example: spawn expects arity 0
// fn bad(x: Int) -> Int ::
//     return x
// ::
// task.spawn(bad)
