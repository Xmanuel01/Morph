// Expected: prints 7
let ch := chan.make()

fn sender() -> Int ::
    chan.send(ch, 7)
    return 0
::

let h := task.spawn(sender)
print(chan.recv(ch))
task.join(h)

// Error example: recv expects a Channel
// chan.recv(1)
