// Lesson 22: Tokenizer training
// Expected: prints "hello world"

fn main() ::
    let cfg := json.parse("{\"path\":\"data/sample.txt\",\"vocab_size\":8}")
    let tok := tokenizer.train(cfg)
    let ids := tok.encode("hello world")
    let text := tok.decode(ids)
    print(text)
::

// Common error example:
// let cfg := json.parse("{\"vocab_size\":8}")
// tokenizer.train(cfg) // error: missing path
