[package]
name = "morphrt"
version = "0.3.0"
edition = "2021"
license = "Apache-2.0"
description = "Morph runtime/interpreter."

[dependencies]
morphc = { path = "../morphc" }

[dev-dependencies]
tempfile = "3.10"
