# Scopes

- Globals: top-level `let` definitions.
- Function scope: parameters + locals.
- Block scope: `if`, `while` introduce child scope for type checking.
- Shadowing allowed; globals remain accessible unless shadowed.

