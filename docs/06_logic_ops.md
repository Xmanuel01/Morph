# Logic Ops

- `not expr` flips Bool.
- `expr1 and expr2` short-circuits: if left is false, right is skipped.
- `expr1 or expr2` short-circuits: if left is true, right is skipped.
- Only Bool operands are allowed (type checker).
